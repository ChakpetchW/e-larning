const GOAL_SCOPES = Object.freeze({
    GLOBAL: 'GLOBAL',
    DEPARTMENT: 'DEPARTMENT'
});

const ANNOUNCEMENT_SCOPES = Object.freeze({
    GLOBAL: 'GLOBAL',
    DEPARTMENT: 'DEPARTMENT'
});

module.exports = {
    GOAL_SCOPES,
    ANNOUNCEMENT_SCOPES
};
