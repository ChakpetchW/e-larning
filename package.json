{
  "name": "e-learning-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd elearning-webapp && npm install && npm run build && cd .. && cp -r elearning-webapp/dist ./dist",
    "install": "cd elearning-webapp && npm install && cd ../elearning-api && npm install"
  }
}
